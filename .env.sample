# Server
PORT=5173
HOST=0.0.0.0

# Authentication — if unset, a random passphrase is generated on startup
CHAOS_PASSWORD=coordinate

# Speech-to-text — WhisperLiveKit WebSocket URL (enables dictation button)
WHISPER_URL=ws://default.taila268e5.ts.net:8000/asr
