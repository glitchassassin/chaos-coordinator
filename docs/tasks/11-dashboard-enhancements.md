# Task 11 — Dashboard Enhancements (Phase 2)

## Goal

Enhance the global dashboard with provider-sourced data and grouping options.

## Subtasks

- [ ] Agent cards show linked issue/PR title (fetched from provider, not just the ID)
- [ ] Agent cards show resolved project name from provider (owner/repo)
- [ ] Group-by-project toggle on global dashboard (flat list vs grouped)
- [ ] Agents launched from issue/PR views (Task 10) auto-populate `linked_issue_id` / `linked_pr_id`

## Acceptance

- Dashboard agent cards show richer context from providers
- Can toggle between flat and grouped agent views
- Linked issue/PR info flows through from launch to display

## References

- Requirements: §2.4
